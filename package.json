{
  "name": "jsonServer_API",
  "version": "1.0.0",
  "description": "jsonServer_API",
  "main": "index.js",
  "scripts": {
    "start": "echo \"Error: no test specified\" && exit 1",
    "json-server": "json-server --watch db.json",
    "json-server:port": "json-server --watch db.json --port 4000",
    "json-server:router": "json-server --watch db.json --port 4000 --routes routes.json",
    "json-server:data": "json-server --watch src/data.js --port 4000 --routes routes.json",
    "json-server:faker1": "json-server --watch src/faker.js --port 4000 --routes routes.json",
    "json-server:faker": "json-server  --watch faker.js",
    "json-server:pais": "json-server --watch src/country.json --port 4000"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/adilsonfuta/jsonServer_API.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/adilsonfuta/jsonServer_API/issues"
  },
  "homepage": "https://github.com/adilsonfuta/jsonServer_API#readme",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "json-server": "^0.17.1"
  }
}
